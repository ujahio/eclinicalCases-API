# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org: eccs-labs
# "app" enables Serverless Framework Dashboard features and sharing them with other Services.
app: clinical
# "service" is the name of this project. This will also be added to your AWS resource names.
service: clinical-backend

plugins:
  - serverless-dynamodb-local
  - serverless-s3-local
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 8080

  dynamodb:
    stages:
      - dev
    start:
      port: 8000
      inMemory: true
      heapInitial: 200m
      heapMax: 1g
      migrate: true
      seed: true
      convertEmptyValues: true
    seed:
      local:
        sources:
          - table: usersTable

  s3:
    bucket: my-bucket
    region: us-east-1
    host: localhost
    port: 4599
    accessKey: "ACCESS_KEY"
    secretKey: "SECRET_KEY"

# stages:
#   default:
#     params:
#       tableName: "users-table-${sls:stage}"

provider:
  name: aws
  runtime: nodejs20.x
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource:
            - Fn::GetAtt: [UsersTable, Arn]
  # environment:
  #   USERS_TABLE: ${param:tableName}

functions:
  api:
    handler: handler.handler
    events:
      - httpApi: "*"
    timeout: 300
  auth:
    handler: handler.handler
    events:
      - http: "POST /api/auth/signin"
      - http: "POST /api/auth/signup"
      - http: "POST /api/auth/send-otp"
      - http: "POST /api/auth/reset-password"
      - http: "POST /api/auth/update-password"
      - http: "GET /api/auth/users"
  cases:
    handler: handler.handler
    events:
      - http: "GET /api/case/details/{proxy+}"
      - http: "GET /api/case/all/"
      - http: "GET /api/case/ongoing-case/"
      - http: "POST /api/case/add"
      - http: "POST /api/case/update/{proxy+}"
      - http: "POST /api/case/duplicate"
      - http: "POST /api/case/publish/"
      - http: "POST /api/case/add/feedback/"
      - http: "GET /api/case/feedbacks/{proxy+}"
      - http: "GET /api/case/responses/{proxy+}"
      - http: "GET /api/case/data/{proxy+}"
      - http: "GET /api/case/student/attempts/{proxy+}"
      - http: "DELETE /api/case/delete-case/{proxy+}"
      - http: "DELETE /api/case/delete/all/"
  quiz:
    handler: handler.handler
    events:
      - http: "POST /api/quiz/submit"
      - http: "GET /api/quiz/answers/{proxy+}"
  student:
    handler: handler.handler
    events:
      - http: "GET /api/student/certificates"
      - http: "GET /api/student/certificate/{proxy+}"

resources:
  Resources:
    usersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: usersTable
        AttributeDefinitions:
          - AttributeName: email
            AttributeType: S
        KeySchema:
          - AttributeName: email
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1

    LocalBucket:
      Type: "AWS::S3::Bucket"
      Properties:
        BucketName: local-bucket
