name: Deploy SST App to Staging

on:
  push:
    branches:
      - staging

permissions:
  id-token: write # This is required for requesting the JWT
  contents: read # This is required for actions/checkout

env:
  AWS_REGION: us-east-1

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Print debug information
        run: |
          if [ -z "${{ secrets.AWS_ACCOUNT_ID }}" ]; then
            echo "AWS_ACCOUNT_ID is not set"
          else
            echo "AWS_ACCOUNT_ID is set"
          fi

      # - name: Checkout repository
      #   uses: actions/checkout@v4

      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: "20"

      # - name: Install dependencies
      #   run: npm install

      # - name: Install SST CLI
      #   run: npm install -g sst

      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/gh-deployment-stg
      #     role-session-name: aws-get-session-token
      #     aws-region: ${{ env.AWS_REGION }}

      # - name: Deploy to Staging
      #   run: sst deploy --stage staging
